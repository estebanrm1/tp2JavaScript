let numero, repeticiones;

for (numero = 1; numero <= 30; numero++) {
    for (repeticiones = 0; repeticiones < numero; repeticiones++) {
        document.write(numero);
    }
    document.write(`<br>`);
}